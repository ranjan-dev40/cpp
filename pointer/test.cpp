#include<bits/stdc++.h>
using namespace std;

int main(){
    // float f=10.5;
    // float p=2.5;
    // float *ptr = &f;
    // (*ptr)++;
    // *ptr = p;
    // cout<<*ptr<<" "<<f<<" "<<p;
    // int *ptr = 0;
    // int a = 10;
    // *ptr = a;
    // cout<<*ptr<<endl;

    // int a=7;
    // int *c = &a;
    // c =  c + 3;
    // // cout<<a<<" "<<*c<<endl;
    // cout<< c <<endl;

    //  //Array pointer

    // int a[4] ={4,5,6,7};
    // // // cout<<*(a+2);
    // // int *p = a++; //syntax error
    // // cout<<*p<<endl;
    // int *p = (a+1);
    // char b[] ="xyz";
    // char *c = &b[0];
    // // cout << b << endl;
    // cout<<c<<endl;

    // char s[]="hello";
    // char *p = s;
    // cout<<s[0] << " "<<p[0];
    // char arr[20];
    // int i;
    // for( i=0; i<10; i++){
    //     *(arr+i) = 65+i;
    // }
    //  *(arr+i) = '\0';
    //  cout<<arr;
    // int numbers[5];
    // int *p;
    // p = numbers;
    // *p = 10;
    // p = &numbers[2];
    // *p = 20;
    // p--;
    // *p = 30;
    // p = numbers + 3;
    // *p = 40;
    // p = numbers;
    // *(p+4) = 50;
    // for(int n=0; n<5; n++){
    //     cout<<numbers[n]<<" ";
    // }

    // char st[] = "ABCD";
    // for(int i=0; st[i] !='\0';i++){
    //     cout<<st[i] << " " <<*(st)+i << " " <<*(i+st)<< " "<<i[st] << endl;
    // }


    
    return 0;
}